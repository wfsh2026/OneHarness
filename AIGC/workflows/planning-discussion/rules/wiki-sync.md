# Wiki 同步规则

## 写入通用 wiki

满足以下条件时，写入 `AIGC/wiki/common/candidates/`：

- 与具体项目无关。
- 可被多个项目复用。
- 有明确来源。
- 低频变化。

## 写入目标项目 wiki

满足以下任一条件时，写入目标项目 wiki：

- 项目目标。
- 项目约束。
- 项目决策。
- 项目文档结构。
- 项目工作流运行事实。

## 禁止

- 不允许把项目事实写入通用 wiki。
- 不允许把未确认内容写入 accepted。

