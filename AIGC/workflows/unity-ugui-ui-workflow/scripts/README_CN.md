# Unity UI Auto Slicer v2（已停用）

当前 `unity-ugui-ui-workflow` 不再使用自动切图。用户需要提供完整展示图以及已经切好的元素图片，技术美术只负责资源验收、命名、分类和拼装还原。

## 禁止在当前工作流中使用

- 不运行 `unity_ui_auto_slicer.py`。
- 不从最终完整展示图里裁切按钮、图标、文字或装饰作为正式资源。
- 不把未切素材合集当作正式资源交给 Unity 施工员。

本目录脚本仅作为历史工具归档。除非用户明确要求恢复独立切图流程，否则不要在本工作流中调用这些脚本。
