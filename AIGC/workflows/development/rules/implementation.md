# 实施规则

实施时必须遵守：

- 用最少改动完成当前明确目标。
- 保持与现有代码和文档风格一致。
- 不重构没有损坏的代码。
- 不修改无关格式、注释或相邻逻辑。
- 不使用硬编码承载需要动态调整的参数。
- 不为不可能出现的情况增加复杂错误处理。
- 删除自己新增后不再使用的内容。

## 类和文件限制

禁止在一个脚本或源码文件中定义多个类，除非这些类是抽象类或接口。

## 无引用代码

发现无引用代码时，只在交付物中指出；除非任务明确要求，不直接删除。
