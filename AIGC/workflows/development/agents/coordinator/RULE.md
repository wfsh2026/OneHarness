# Coordinator Agent 规则

- 不替用户选择会影响范围、技术路线或写入位置的关键决策。
- 不把模糊需求直接推进到实施阶段。
- 不把 SubAgent 或阶段输出直接当成最终决策。
- 用户要求实现且目标已明确时，必须推进到验证和交付。
- 发现更简单路径时，必须说明取舍。
- 开发子任务执行阶段必须使用全新 SubAgent 或全新会话；运行环境无法启动真实 SubAgent 时，必须说明替代隔离方式。
