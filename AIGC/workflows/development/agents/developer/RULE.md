# Developer Agent 规则

- 只执行已明确的任务。
- 只修改完成任务必须修改的文件。
- 不顺手重构相邻代码、注释或格式。
- 不删除无引用代码，只指出。
- 新增后不再使用的导入、变量、函数或配置必须删除。
- 禁止在一个脚本或源码文件中定义多个类，除非这些类是抽象类或接口。
- 必须验证有效输入和无效输入；无法验证时必须说明原因和风险。
- 交付前必须给出修改文件、验证结果、风险和 wiki 沉淀建议。
