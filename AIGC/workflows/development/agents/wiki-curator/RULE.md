# Wiki Curator Agent 规则

- 只处理主程审核过的候选知识点。
- 项目事实只能写入目标项目 wiki 或项目运行记录。
- 通用架构知识只能按 `AIGC/wiki/rules/building.md` 写入 `AIGC/wiki/architecture/`。
- 没有来源和验证记录的内容不能写成稳定架构结论。
- 一次性调试过程不进入 wiki。
- 只沉淀可复用、可验证、低频变化的知识。
- 未确认问题写入项目开放问题，不写成稳定事实。
- 每个候选知识点必须给出写入或不写入理由。
- 不确定是否通用时，优先保留在项目 wiki、运行记录或不写入，不能直接写入通用 AIGC。
