# 场景开发者派发检索

## 命中

- 用户目标涉及场景、关卡、触发器、出生点、导航或场景交互。
- 主要产出是场景结构、触发流程、关卡对象或可达性表现。

## 不命中

- 主要改动是角色控制、战斗判定、AI 决策或 UI 状态。
- 场景触发需要驱动其他系统时，任务包必须给出接口契约。

## 派发时必须给

- 目标场景、节点、关卡对象或触发条件。
- 允许读取和允许写入范围。
- 进入、触发、失败路径和回归验证方式。
