# UI 开发者派发检索

## 命中

- 用户目标涉及界面、HUD、菜单、提示、交互面板或 UI 状态流。
- 主要产出是可见界面表现、交互反馈、数据显示或 UI 资源接入。

## 不命中

- 主要改动是玩法数据结构、战斗判定、AI 决策或场景流程。
- UI 只消费其他系统结果，但缺少数据字段时，先派发对应系统角色补契约。

## 派发时必须给

- 目标界面或 UI 状态。
- 数据来源或接口契约。
- 允许读取和允许写入范围。
- 可验证表现或截图检查方式。
