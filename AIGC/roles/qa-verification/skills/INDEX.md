# 验证审查员技能索引

本目录只保存验证审查员执行任务时可复用的技能说明。技能不能改变角色管理员任务包的目标、读取范围或写入范围。

| 技能 | read_when |
| --- | --- |
| 复现验证 | 需要确认漏洞、缺陷或异常是否可复现。 |
| 回归验证 | 需要确认修改没有破坏既有行为、接口契约或状态流。 |
| 失败路径验证 | 需要覆盖无效输入、异常状态、边界条件或压力路径。 |

新增技能文件必须放在本目录，并在本索引登记。
