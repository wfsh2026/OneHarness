# 场景开发者工具索引

本目录只保存场景开发者执行任务时可使用的工具调用说明。工具只能在角色管理员任务包允许范围内使用。

| 工具类别 | read_when |
| --- | --- |
| 文件检索 | 需要定位场景、关卡、触发器、导航或出生点相关入口。 |
| 引擎或编辑器工具 | 需要检查或修改场景层级、节点、Prefab、导航或触发器。 |
| 运行路径检查 | 需要验证进入、触发、可达性、失败路径或回归表现。 |

新增工具说明必须放在本目录，并在本索引登记。
