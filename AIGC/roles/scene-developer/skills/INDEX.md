# 场景开发者技能索引

本目录只保存场景开发者执行任务时可复用的技能说明。技能不能改变角色管理员任务包的目标、读取范围或写入范围。

| 技能 | read_when |
| --- | --- |
| 场景结构调整 | 需要调整场景层级、关卡对象、出生点或环境交互。 |
| 触发器和流程接入 | 需要实现进入、离开、触发、区域事件或场景流程。 |
| 导航和路径验证 | 需要处理导航、路径、阻挡、可达性或场景失败路径。 |

新增技能文件必须放在本目录，并在本索引登记。
