# 3C 开发者工具索引

本目录只保存 3C 开发者执行任务时可使用的工具调用说明。工具只能在角色管理员任务包允许范围内使用。

| 工具类别 | read_when |
| --- | --- |
| 文件检索 | 需要定位角色控制、输入、镜头或动画状态相关入口。 |
| 引擎运行检查 | 需要验证移动、输入响应、镜头表现或动画状态切换。 |
| 调试输出检查 | 需要确认输入事件、状态变化、速度、约束或动画参数。 |

新增工具说明必须放在本目录，并在本索引登记。
