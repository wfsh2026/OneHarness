# AI 开发者工具索引

本目录只保存 AI 开发者执行任务时可使用的工具调用说明。工具只能在角色管理员任务包允许范围内使用。

| 工具类别 | read_when |
| --- | --- |
| 文件检索 | 需要定位感知、行为、状态机、行为树、导航或目标选择入口。 |
| 运行调试 | 需要验证 AI 感知、决策、路径、状态切换或异常行为。 |
| 日志和状态检查 | 需要确认目标选择、仇恨变化、决策条件或失败原因。 |

新增工具说明必须放在本目录，并在本索引登记。
