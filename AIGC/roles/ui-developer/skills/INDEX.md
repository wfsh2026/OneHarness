# UI 开发者技能索引

本目录只保存 UI 开发者执行任务时可复用的技能说明。技能不能改变角色管理员任务包的目标、读取范围或写入范围。

| 技能 | read_when |
| --- | --- |
| UI 状态实现 | 需要实现空态、加载态、错误态、成功态或状态切换表现。 |
| UI 数据展示接入 | 需要把任务包允许的数据、状态或事件结果展示到界面。 |
| UI 交互反馈 | 需要实现点击、悬停、提示、禁用态、确认反馈或动效反馈。 |

新增技能文件必须放在本目录，并在本索引登记。
