# 项目适配配置模板

```yaml
project_id:
display_name:
source_project_root:
capsule_root:
active_workflow:
project_wiki_root:
workflow_runs_root:
created_at:
```

本模板用于 `AIGC/_local/registry.yaml` 或项目胶囊内的 `ADAPTER.md`。真实本地路径只能写入被 Git 忽略的本机配置或项目胶囊。
