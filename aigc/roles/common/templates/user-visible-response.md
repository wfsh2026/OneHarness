# 主会话用户可见回复模板

本模板只提供主会话输出结果骨架。字段含义、适用范围和禁止项以 `../session-visible-state.md` 为准。

```text
【当前角色】角色管理员
【主要职责】{角色管理员在当前会话或当前阶段承担的沟通、派发、回收或交付判断职责}
【工作依据】{实际读取或命中的规则文件、模板文件、用户确认输入或产物路径}

【详细信息】
{当前回复内容}
```

## 使用要求

- 不在本模板重复维护固定格式规则。
- 本模板变化时，必须确认 `../session-visible-state.md` 仍是唯一权威。
