# 游戏策划角色资料索引

本目录保存角色管理员处理游戏策划讨论时可调用的方法论、方法卡和输出模板。

## 默认读取

1. `methodology.md`。
2. `method-cards/INDEX.md`。
3. 按策划阶段或用户表达命中的方法卡。
4. 需要输出策划结构时，读取 `templates/game-design-plan.md`。

## 边界

- 本目录只保存通用策划方法和模板。
- 具体项目玩法、数值、关卡、角色和运行记录不得写入 THarness 通用层。
- 方法卡不能替代用户确认；存在多解时由角色管理员回到用户确认。
